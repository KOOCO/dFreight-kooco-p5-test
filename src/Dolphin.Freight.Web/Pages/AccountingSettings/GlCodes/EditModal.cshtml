@page
@using Dolphin.Freight.Localization
@using Dolphin.Freight.Web.Pages.Components
@using Microsoft.Extensions.Localization
@using Volo.Abp.AspNetCore.Mvc.UI.Bootstrap.TagHelpers.Modal
@model Dolphin.Freight.Web.Pages.AccountingSettings.GlCodes.EditModalModel
@inject IStringLocalizer<FreightResource> L
@{
    Layout = null;
    <abp-script src="/Pages/AccountingSettings/GlCodes/init.js" />
}
<abp-modal>
    <abp-modal-header title="@L["Create"].Value"></abp-modal-header>
    <abp-modal-body>
        <abp-row>
            <abp-column>
                <abp-input asp-for="GlCode.Code" label="@L["GlCode"]" />
            </abp-column>
        </abp-row>
        <abp-row>
            <abp-column>
                @await Component.InvokeAsync(typeof(SelectComponent),new {TagName =  "GlTypeId", FieldName = "GlTypeId",DefaultValue="", IsShowLabel = true , IsRequired = false  })
            </abp-column>
        </abp-row>

        <abp-row>
            <abp-column>
                @await Component.InvokeAsync(typeof(SelectComponent),new {TagName =  "GlGroupId", FieldName = "GlGroupId",DefaultValue="", IsShowLabel = true , IsRequired = false  })
            </abp-column>
        </abp-row>
        <abp-row>
            <abp-column>
                <abp-input asp-for="GlCode.Remark" />
            </abp-column>
        </abp-row>
        <abp-row>
            <abp-column>
                <abp-input asp-for="GlCode.SubInfo" />
            </abp-column>
        </abp-row>
        <abp-row>
            <abp-column>
                <abp-input asp-for="GlCode.AccountingGlCode" />
            </abp-column>
        </abp-row>
        <abp-row>
            <abp-column>
                <abp-input asp-for="GlCode.IsUsed" />
                <abp-input asp-for="GlCode.IsDeposit" />
            </abp-column>
        </abp-row>
        <abp-row>
            <abp-column>

                <abp-input asp-for="GlCode.IsPayment" />
                <abp-input asp-for="GlCode.IsRevaluation" />
            </abp-column>
        </abp-row>
    </abp-modal-body>
    <abp-modal-footer>
        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
            <abp-button class="btn btn-primary me-md-2" type="button" id="saveBtn">@L["Save"].Value</abp-button>
        </div>
        <form id="createForm" method="post" action="/AccountingSettings/GlCodes/EditModal">
            <abp-input asp-for="Id" />
            <input type="hidden" name="Code" id="formCode" value="@Model.GlCode.Code" />
            <input type="hidden" name="GlTypeId" id="formGlTypeId" value="@Model.GlCode.GlTypeId" />
            <input type="hidden" name="GlGroupId" id="formGlGroupId" value="@Model.GlCode.GlGroupId"  />
            <input type="hidden" name="Remark" id="formRemark" value="@Model.GlCode.Remark"  />
            <input type="hidden" name="SubInfo" id="formSubInfo" value="@Model.GlCode.SubInfo" />
            <input type="hidden" name="AccountingGlCode" id="formAccountingGlCode" value="@Model.GlCode.AccountingGlCode" />
            <input type="hidden" name="IsUsed" id="formIsUsed" value="@Model.GlCode.IsUsed" />
            <input type="hidden" name="IsDeposit" id="formIsDeposit" value="@Model.GlCode.IsDeposit" />
            <input type="hidden" name="IsPayment" id="formIsPayment" value="@Model.GlCode.IsPayment"  />
            <input type="hidden" name="IsRevaluation" id="formIsRevaluation" value="@Model.GlCode.IsRevaluation" />
        </form>
    </abp-modal-footer>
</abp-modal>

